# ============================================================================
# 운영 안정화 패키지 (Option A) 환경 변수
# 생성일: 2025-10-31
# ============================================================================

# Low-Confidence 가드레일
LOW_CONF_DELTA=0.05          # Score delta threshold (초기: 0.05, 모니터링 후 0.07까지 상향 검토)
LOW_CONF_MIN_HITS=1           # Minimum hits required for confidence check
LOW_CONF_LIST_SIZE=20         # LIST_FIRST 모드에서 표시할 문서 개수

# 금액 추출 검증
FINANCE_TOLERANCE_PCT=0.05    # ±5% 허용 오차 (unit_price×qty vs amount 등)

# OCR 설정
OCR_MIN_AVG_CHARS_PER_PAGE=300  # 페이지당 평균 글자수 임계값 (이미 ingest_from_docs.py에 반영됨)

# 메트릭스 & 알림 임계치 (향후 구현용)
# ALERT_LOW_CONF_RATE=0.25          # low_conf_triggered / total_queries ≥ 25%
# ALERT_LATENCY_P95_MS=3000         # retrieval_latency_ms_p95 ≥ 3000ms
# ALERT_HIT_RATE_MIN=0.80           # exact_match_hit_rate ≤ 80%
# ALERT_STALE_INDEX_MAX=0           # stale_index_entries > 0
# ALERT_FS_INDEX_DIFF_MAX=5         # |fs_file_count - index_file_count| > 5

# ============================================================================
# 사용 방법:
# 1. 기존 .env 파일이 있다면 아래 내용을 추가
# 2. 없다면 이 파일을 .env로 복사: cp .env.ops_stabilization .env
# 3. 서비스 재시작
# ============================================================================
