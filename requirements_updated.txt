# AI-CHAT-V3 Requirements - Updated for Migration
# Python 3.9+ required (tested with 3.12.3)

# 🔥 Core Dependencies - 정확한 버전
streamlit>=1.49.0
pdfplumber>=0.11.0
llama-cpp-python>=0.2.90

# 🤖 NLP & Embeddings - Updated versions
sentence-transformers>=5.0.0
transformers>=4.30.0
konlpy>=0.6.0
kiwipiepy>=0.15.0  # 한국어 형태소 분석 (BM25용)

# 🔍 Vector Search - Updated versions
faiss-cpu>=1.12.0
rank_bm25>=0.2.2

# 📊 Data Processing - Updated versions
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# 📄 PDF & Image Processing
pypdf2>=3.0.0
pillow>=10.0.0

# 🛠️ Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
pathlib  # Python 표준 라이브러리
typing-extensions>=4.5.0

# 🌐 Optional: Model Download
huggingface-hub>=0.20.0  # 모델 다운로드용 (선택사항)

# 🖥️ System Dependencies (설치 스크립트에서 처리)
# Linux/WSL2: build-essential python3-dev libffi-dev
# Windows: Visual C++ Build Tools (자동 설치됨)

# ❌ 제외된 패키지들 (문제가 있거나 불필요)
# torch  # CPU 전용이므로 불필요, 크기가 큼
# torchvision  # 불필요
# mecab-python3  # 시스템 의존성 복잡

# 🎯 Production Verified Versions (2025-09-10 기준)
# - Python: 3.12.3
# - streamlit: 1.49.1  
# - sentence-transformers: 5.1.0
# - faiss-cpu: 1.12.0
# - pdfplumber: 0.11.7

# 📝 Installation Notes:
# 1. 가상환경 사용 필수: python3 -m venv ai-chat-env
# 2. 업그레이드 명령어: pip install --upgrade pip setuptools wheel
# 3. 의존성 설치: pip install -r requirements.txt
# 4. 모델 다운로드: python3 download_models.py

# ⚡ Performance Tips:
# - CPU 코어 수에 따른 n_ctx 조정
# - 메모리 8GB 이상 권장 (16GB 최적)
# - SSD 사용시 인덱싱 속도 3배 향상