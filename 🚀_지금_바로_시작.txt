═══════════════════════════════════════════════════════════
🚀 지금 바로 시작하기 (3단계로 끝!)
═══════════════════════════════════════════════════════════

📌 이 파일만 따라하세요! 35분이면 끝!

═══════════════════════════════════════════════════════════


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1단계: WSL 설치 (10분)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① Windows 키 누르고 "PowerShell" 입력
② 우클릭 → "관리자 권한으로 실행"
③ 아래 명령어 복사-붙여넣기:

   wsl --install

④ 재시작:

   Restart-Computer

⑤ 재시작 후 Ubuntu 창이 열리면:
   - 사용자 이름 입력: wnstn4647
   - 비밀번호 입력 (화면에 안 보여도 입력됨!)

✅ 완료!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2단계: 환경 설정 (15분)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① Ubuntu 터미널 열기 (Windows 검색 → "Ubuntu")

② 아래 명령어 한 줄씩 복사-붙여넣기:

   sudo apt update && sudo apt upgrade -y

   sudo apt install -y build-essential wget curl git unzip software-properties-common

   sudo add-apt-repository ppa:deadsnakes/ppa -y

   sudo apt update

   sudo apt install -y python3.10 python3.10-venv python3.10-dev python3-pip

   sudo apt install -y tesseract-ocr tesseract-ocr-kor libtesseract-dev poppler-utils

✅ 완료!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3단계: 프로젝트 설치 (10분)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① Ubuntu 터미널에서:

   cp -r "/mnt/c/Users/wnstn/OneDrive/Desktop/AI/AI-CHAT" ~/AI-CHAT

   cd ~/AI-CHAT

   chmod +x *.sh

   bash SETUP_NEW_PC.sh

   (이 스크립트가 모든 패키지를 자동 설치! 5-10분 소요)

② 설치 완료 후 실행:

   source .venv/bin/activate

   streamlit run web_interface.py --server.port 8501

③ 브라우저에서 열기:

   http://localhost:8501

✅ 완료! 🎉


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
다음부터는 이렇게 실행하세요
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① Ubuntu 터미널 열기

② 명령어:

   cd ~/AI-CHAT
   source .venv/bin/activate
   streamlit run web_interface.py --server.port 8501

또는 간단하게:

   cd ~/AI-CHAT
   bash start_ai_chat.sh


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
문제 생기면?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 "새PC_완벽설치.md" 파일 보기 (상세 가이드)
📖 "문제해결.md" 파일 보기 (트러블슈팅)


═══════════════════════════════════════════════════════════
끝! 이제 시작하세요! 🚀
═══════════════════════════════════════════════════════════
